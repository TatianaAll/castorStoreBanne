<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ title }}</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
  <style>
    .wf-container { padding-top: 2.5rem; padding-bottom: 2.5rem; }
    .wf-title-bar { background:#e9ecef; border-radius:.5rem; padding:.85rem 1.25rem; }
    .wf-panel { background:#f1f3f5; border-radius:.75rem; padding:2rem; }
    .wf-section { margin-top:2.5rem; margin-bottom:2.5rem; }
    .wf-products-wrap { background:#e9ecef; border-radius:.75rem; padding:2rem; }
    .wf-heading { font-weight:600; }
    .wf-img-400 { width:100%; aspect-ratio:1/1; object-fit:cover; border-radius:.5rem; }
    @supports not (aspect-ratio:1/1) { .wf-img-400{height:400px;} }
  </style> 
</head>
<body>
  <main class="container wf-container">
    <div class="row wf-section">
      <div class="col-12 col-md-8 offset-md-2">
        <div class="wf-title-bar text-center">
          <h1 class="h3 m-0">{{ title }}</h1>
        </div>
      </div>
    </div>

    <section id="about" class="row wf-section">
      <div class="col-12 col-md-10 offset-md-1">
        <div class="wf-panel text-center">
          <h2 class="h5 wf-heading mb-3">Qui sommes nous</h2>
          <p class="mb-0">{{ quiSommesNous }}</p>
        </div>
      </div>
    </section>

    <section id="products" class="row wf-section">
      <div class="col-12 col-md-6 offset-md-3">
        <div class="wf-title-bar text-center">
          <h2 class="h6 m-0">Nos produits</h2>
        </div>
      </div>

      <div class="col-12 mt-4">
        <div class="wf-products-wrap">
          <div class="row row-cols-1 row-cols-sm-2 row-cols-lg-3 g-4">
            {% for product in products %}
              <div class="col">
                <div class="card h-100 shadow-sm border-0">
                  <a href="{{ path('product_show', { id: product.id }) }}" class="d-block">
                      <img src="{{ asset(product.image) }}" alt="" class="wf-img-400" width="400" height="400" loading="lazy">
                  </a>
                  <div class="card-body d-flex flex-column">
                    <h3 class="h6 mb-2">{{ product.name }}</h3>
                    <div class="mt-auto fw-semibold">{{ product.price|number_format(2, '.', ',') }} €</div>
                    <a href="{{ path('product_show', { id: product.id }) }}" class="btn btn-primary btn-sm mt-2">Voir le produit</a>
                  </div>
                </div>
              </div>
            {% endfor %}
          </div>
        </div>
      </div>
    </section>
  </main>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
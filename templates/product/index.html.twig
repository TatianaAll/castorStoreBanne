<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ title is defined ? title : 'Produits' }}</title>

  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
  <style>
    .wf-container { padding-top: 2.5rem; padding-bottom: 2.5rem; }
    .wf-title-bar { background:#e9ecef; border-radius:.5rem; padding:.85rem 1.25rem; }
    .wf-section { margin-top:2.5rem; margin-bottom:2.5rem; }
    .wf-img-400 { width:100%; aspect-ratio:1/1; object-fit:cover; border-radius:.5rem; }
    @supports not (aspect-ratio:1/1) { .wf-img-400{height:400px;} }
  </style>
</head>
<body>
<main class="container wf-container">

  <div class="row wf-section">
    <div class="col-12 col-md-6 offset-md-3">
      <div class="wf-title-bar text-center">
        <h1 class="h4 m-0">{{ title is defined ? title : 'Produits' }}</h1>
      </div>
    </div>
  </div>

  {% for product in products %}
    <div class="card mb-4 shadow-sm border-0">
      <div class="row g-0 align-items-center">
        <div class="col-12 col-md-5 col-lg-4 p-3">
            <img src="{{ asset(product.image) }}" class="wf-img-400 w-100" alt="{{ product.name }}">
        </div>
        <div class="col-12 col-md-7 col-lg-8">
          <div class="card-body">
            <h2 class="h5">{{ product.name }}</h2>
            <div class="text-primary fw-semibold mb-2">{{ product.price|number_format(2, '.', ',') }} €</div>
            <p class="mb-3">{{ product.description }}</p>
            <a href="{{ path('product_show', { id: product.id }) }}" class="btn btn-primary">
              Voir le produit
            </a>
          </div>
        </div>
      </div>
    </div>
  {% else %}
    <p class="text-center text-muted">Aucun produit pour le moment.</p>
  {% endfor %}

</main>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>